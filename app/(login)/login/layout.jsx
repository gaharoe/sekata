"use client"

import "../../globals.css"

export default function LoginLayout({children}){
    return (
        <html>
            <body>
                {children}
            </body>
        </html>
    )
}